export enum UniversityType {
    PUBLIC = 'public',
    PRIVATE = 'private',
}

export enum ClassSize {
    SMALL = 'small',
    FLEXIBLE = 'flexible',
}

export enum Gradation {
    LOW = 'low',
    MEDIUM = 'medium',
    HIGH = 'high',
}

export enum Shift {
    ONE_SHIFT = 'one_shift',
    FULL_TIME = 'full_time',
    FLEXIBLE = 'flexible',
}
